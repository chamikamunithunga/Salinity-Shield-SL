// Configuration for Salinity Shield
// Note: Firebase Cloud Function is now called via Firebase SDK (see firebase.js)
// The function URL is automatically resolved based on the Firebase project configuration

// Default Reach ID for Kalu Ganga - Update with actual NASA Reach ID
// You can set this via environment variable: VITE_REACH_ID
export const DEFAULT_REACH_ID = import.meta.env.VITE_REACH_ID || 'your-reach-id'

